[package]
name = "voxora-backend"
# Pinned to 0.2.0: cross-cutting hardware backend (CUDA → Metal → CPU
# dispatcher). The 0.2.0 cut is the coordinated Fase 2 release.
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Cross-cutting hardware backend selection for voxora ASR engines"
authors.workspace = true
rust-version.workspace = true
keywords.workspace = true
categories.workspace = true
publish = true
readme = "README.md"

[dependencies]
voxora-core   = { workspace = true }
voxora-engine = { workspace = true }
thiserror     = { version = "2", default-features = false }

[dev-dependencies]
tempfile = "3"